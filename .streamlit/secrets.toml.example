# Copy this to .streamlit/secrets.toml for local development,
# OR paste the content into Streamlit Cloud's "Secrets" panel for deployment.
#
# When unset, the password gate is bypassed (useful for local dev).

password = "change-me-something-only-you-and-dad-know"

# Optional: EIA API key for the oil-flow panel.
# EIA_API_KEY = "your-free-key-from-eia.gov-opendata"
